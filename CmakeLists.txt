cmake_minimum_required(VERSION 2.8.9)
project(Machine_Learning)
file(GLOB SOURCES "src/*")
add_executable(ml ${SOURCES})

# Include the path of packages in main.cpp
include_directories(Algorithm_Packages)
include_directories(Helper_Packages)
include_directories(Deep_Learning)
# Define the location of packages containing the cmakelist
add_subdirectory (Algorithm_Packages)
add_subdirectory (Helper_Packages)

target_link_libraries (ml PUBLIC Helper_Packages Algorithm_Packages)
