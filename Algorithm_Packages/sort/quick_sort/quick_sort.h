/* Algorithm:
	Take the last element of an array as a pivot and put smaller elements before the pivot, and greater elements after the pivot.
	Repeat it till no values can be partitioned.*/
void quick_sort(int arr[], int low, int high);
